import "../styles/App.css";

const RetryOutputButton = () => {
    <div>
    <p className="">Documentation failed to generate :(</p>
    <button onClick={onGenerateOutputFile} className="btn-theme red">Click here to regenerate</button>
    </div>
}

export default RetryOutputButton;